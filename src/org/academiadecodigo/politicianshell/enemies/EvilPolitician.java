package org.academiadecodigo.politicianshell.enemies;


//public class EvilPolitician extends Enemy {
/*
    private int curruptionLevel;

    public EvilPolitician(EnemyType enemyType, int health, int curruptionLevel){
        super(enemyType, health);
        this.curruptionLevel = curruptionLevel;
    }

    @Override
    public void hit (Weapon weapon){

        if (curruptionLevel > WeaponType.getBullets(){
            curruptionLevel -= WeaponType.getBullets();
            return;
        }

        WeaponType.getBullets() -= curruptionLevel;
        curruptionLevel = 0;
        super.hit(WeaponType.getBullets());
    }
*/
//}

